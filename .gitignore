__pycache__/
*.pyc
tgctl.log

# Never commit these. They live in $HOME, but a stray copy here would carry the
# bot token, which is a shell on this machine — see SECURITY.md.
.tgctl.json
tgctl.json
*.offset
